# AI-Driven-Communication-Surveillance
An intelligent compliance surveillance tool that reads emails from an Excel file, asks Azure OpenAI (via LangChain) to judge compliance, parses the model’s JSON, assigns a priority score per category, and shows the results in a sorted table with a summary chart.
